
namespace Unix;

public class UnixBot
{
    // If I ever need my own instance of the IApiClient(for maybe some custom sharding logic)
    // Then I'll use this, for now it will remain unused.
}